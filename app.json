{
  "name": "Node.js Sample",
  "env": {
    "REDISENDPOINT_URL": {
      "description": "A Redis cloud endpoint URL.",
      "required": "true"
    },
    "REDIS_PASSWORD": {
      "description": "A Redis password.",
      "required": "true"
    }
  }
}
